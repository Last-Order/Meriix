const state = {
    dropHelperOptions: []
};

const getters = {};
const actions = {};
const mutations = {
    setDropHelperOptions(state, payload){
        state.dropHelperOptions = payload;
    }
};

export default {
    state,
    getters,
    actions,
    mutations
}